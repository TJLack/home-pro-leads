# Storage Foundation

This module contains local persistence adapters.

- Current adapter: `LeadRepository` file-backed JSON store for local development.
- Planned upgrade path: move to SQLite-backed repository while keeping the same interface.
